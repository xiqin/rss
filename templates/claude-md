# {{PROJECT_NAME}}

{{PROJECT_DESC}}

> 本项目使用 **rss（Requirement-Driven Software Engineering）** AI 工程化框架。

## 宪法（最高优先级）

- **项目章程**：`.claude/memory/constitution.md` — 核心原则
- **工程约束**：`.claude/rules/project-structure.md` — 目录结构、编码红线

**所有开发活动必须遵守以上两份文件。**

## 入口

| 入口 | 路径 | 用途 |
|------|------|------|
{{ENTRY_POINTS_TABLE}}

## 开发流程

```
brainstorming → writing-plans → git-worktree → subagent-dev → index-update
```

| Step | 阶段 | 说明 |
|------|------|------|
| 1 | brainstorming | 需求头脑风暴，产出 `spec.md` |
| 2 | writing-plans | 按分层拆 task，产出 `plan.md` |
| 3 | git-worktree | 创建隔离分支 |
| 4 | subagent-dev | Subagent 隔离派发 + 双审查 |
| 5 | index-update | 同步工程索引 |

## 快速命令

```bash
# 编译
{{BUILD_CMD}}

# 测试
{{TEST_CMD}}

# 静态检查
{{VET_CMD}}
```

## 工程索引

`ENGINEERING-INDEX.md` 是项目索引，包含路由表、控制器/服务/模型层的方法签名、依赖关系。

## 完成工作后更新

代码变更后同步更新：

1. `ENGINEERING-INDEX.md` — 新增/删除了模块、路由、控制器、服务
2. `.claude/memory/MEMORY.md` — 踩坑、用户偏好、变更要点
3. `CLAUDE.md` — 引入了新的约定或命令

## 记忆

持久化记录在 `.claude/memory/MEMORY.md`，新会话时先读此文件。
